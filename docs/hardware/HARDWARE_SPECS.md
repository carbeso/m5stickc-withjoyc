# M5StickC Plus 與 MiniJoyC 硬體規格與開發參考手冊

本文件彙整 **M5StickC Plus** 核心開發板與 **MiniJoyC HAT** 搖桿擴充底座之硬體規格、腳位映射、I2C 通訊協議與暫存器定義，專門作為後續開發**掌上型紓壓玩具 (Stress Relief / Fidget Toy)** 的核心硬體參考依據。

---

## 1. 系統整體架構

本紓壓玩具採用「主核心 + 協同底座」的雙晶片架構：
- **主核心 (Master)**：M5StickC Plus（搭載 ESP32-PICO-D4），負責主要遊戲邏輯、LCD 彩色畫面繪製、六軸姿態運算、蜂鳴器音效控制與電源管理。
- **擴充底座 (Slave)**：MiniJoyC HAT（搭載 STM32F030F4P6），透過 I2C（位址 `0x54`）提供雙軸類比搖桿讀取、搖桿中心微動按鍵偵測、獨立 SK6812 全彩 RGB LED 效果，並自帶 200mAh 鋰電池輔助供電。

```
+-------------------------------------------------------------+
|                      M5StickC Plus                          |
|  [ESP32-PICO-D4 240MHz]                                     |
|  ├── 1.14" 135x240 ST7789v2 TFT LCD (視覺動畫/UI)           |
|  ├── MPU6886 6軸 IMU (晃動/傾斜/手勢偵測)                  |
|  ├── 無源蜂鳴器 Passive Buzzer (機械敲擊聲/音效)             |
|  ├── AXP192 電源管理 + 120mAh 鋰電池                        |
|  ├── Button A (正面大鍵) & Button B (側面功能鍵)            |
|  └── 頂部 8-Pin HAT 擴充槽 (I2C 通訊)                       |
+------------------------------┬------------------------------+
                               │ I2C Bus (SDA: G0, SCL: G26)
+------------------------------┴------------------------------+
|                       MiniJoyC HAT                          |
|  [STM32F030F4P6 協同處理器 @ I2C 0x54]                     |
|  ├── 雙軸類比搖桿 (XY軸 最大偏角 23°, 10kΩ, 8/10/12-bit)    |
|  ├── 搖桿中心按鍵 (Click Switch)                            |
|  ├── SK6812 3535 全彩 RGB LED (動態氛圍光/按壓回饋)         |
|  └── 內建 200mAh 鋰電池 (增加掌上型便攜續航)                |
+-------------------------------------------------------------+
```

---

## 2. M5StickC Plus (SKU: K016-P) 硬體規格

### 2.1 主要規格參數
| 項目 | 規格參數 | 備註說明 |
| :--- | :--- | :--- |
| **主控 SoC** | ESP32-PICO-D4 @ Xtensa® 32 位元 LX6 雙核心 | 主頻最高 240MHz，運算效能 600 DMIPS |
| **記憶體 (SRAM)** | 520 KB 內部 SRAM | 適合運行雙緩衝繪圖與即時物理碰撞運算 |
| **快閃記憶體 (Flash)** | 4 MB SPI Flash | 存放韌體、圖形資產、字型檔與音效表 |
| **無線網路** | 2.4 GHz Wi-Fi (802.11 b/g/n) + 藍牙 4.2 BR/EDR & BLE | 可用於無線遙控或數據同步 |
| **彩色液晶螢幕 (LCD)** | 1.14 吋 Colorful TFT LCD，解析度 135 × 240 | 驅動晶片：ST7789v2，支援 SPI 介面 |
| **姿態感測器 (IMU)** | MPU6886 (3 軸加速度計 + 3 軸陀螺儀) | I2C 介面 (內部匯流排 `0x68`) |
| **即時時鐘 (RTC)** | BM8563 | I2C 介面 (內部匯流排 `0x51`) |
| **電源管理單元 (PMU)** | AXP192 | 具備精確電壓電流測量、充電管理與多路 LDO/DC-DC |
| **內建電池** | 120 mAh @ 3.7V 鋰聚合物電池 | 支援低功耗深度睡眠喚醒 |
| **聲音回饋** | 無源蜂鳴器 (Passive Buzzer) | 接於 GPIO 2，支援 PWM 頻率與音調產生 |
| **聲音輸入** | SPM1423 數位 PDM 麥克風 | CLK: GPIO 0, DATA: GPIO 34 |
| **指示燈與發射器** | 1 × 內建紅色 LED (GPIO 10) + 1 × 紅外線發射器 IR (GPIO 9) |  |
| **實體按鍵** | Button A (GPIO 37)、Button B (GPIO 39)、電源鍵 (AXP192 PWRON) |  |
| **外部連接埠** | USB Type-C (供電/程式燒錄)、GROVE 連接埠 (HY2.0-4P)、頂部 HAT 槽 |  |
| **外型尺寸與重量** | 48.0 × 24.0 × 13.5 mm，重量約 16.9g | 輕巧便攜，適合單手把玩 |

### 2.2 腳位映射表 (Pinout Mapping)

#### (1) LCD 螢幕 (ST7789v2 - SPI 介面)
| 訊號名稱 | ESP32 GPIO | 說明 |
| :--- | :--- | :--- |
| **TFT_MOSI** | GPIO 15 | SPI 主機輸出資料線 |
| **TFT_CLK** | GPIO 13 | SPI 時脈線 |
| **TFT_DC** | GPIO 23 | 資料 / 指令選擇線 |
| **TFT_RST** | GPIO 18 | 螢幕硬體重置線 |
| **TFT_CS** | GPIO 5 | 片選訊號線 (致能低電位) |
| **TFT_BL** | AXP192 LDO2 | 螢幕背光亮度透過 AXP192 LDO2 電壓無段調整 |

#### (2) 內部 I2C 匯流排 (Wire1 - IMU / RTC / PMU)
| 裝置名稱 | I2C 位址 | SCL 接腳 | SDA 接腳 | 說明 |
| :--- | :--- | :--- | :--- | :--- |
| **AXP192** | `0x34` | GPIO 22 | GPIO 21 | 電源管理單元 (PMU) |
| **MPU6886** | `0x68` | GPIO 22 | GPIO 21 | 六軸姿態感測器 |
| **BM8563** | `0x51` | GPIO 22 | GPIO 21 | 即時時鐘晶片 |

#### (3) 頂部 8-Pin HAT 擴充槽實體接腳映射（依 MiniJoyC 原理圖 Header P1）
| HAT 接腳編號 | 訊號名稱 | ESP32 腳位 | 說明與功能 |
| :--- | :--- | :--- | :--- |
| **PIN 1** | `5VIN` | VBUS_VIN | 外部 5V 供電輸入端（通往 AXP192 充電電路） |
| **PIN 2** | `3V3` | VESP_3V3 | AXP192 輸出的系統 3.3V 電源 |
| **PIN 3** | `BAT` | BAT_IN | 內部鋰電池正極，可與 MiniJoyC 內建電池並聯 |
| **PIN 4** | `SDA` | **GPIO 0** | 外部 I2C 資料線（MiniJoyC 通訊核心） |
| **PIN 5** | `G36/G25` | **GPIO 36 / 25** | 複合腳位（MiniJoyC 內部為 NC 未連接） |
| **PIN 6** | `SCL` | **GPIO 26** | 外部 I2C 時脈線（MiniJoyC 通訊核心） |
| **PIN 7** | `5V` | 5VOUT | StickC Plus 經由升壓晶片輸出的 5V 電源 |
| **PIN 8** | `GND` | GND | 系統接地端 |

#### (4) 內建周邊接腳
| 周邊名稱 | ESP32 腳位 | 觸發模式 / 說明 |
| :--- | :--- | :--- |
| **紅色 LED** | GPIO 10 | 低電位點亮 (Active LOW) |
| **紅外發射 (IR)** | GPIO 9 | 高電位發射 |
| **按鍵 Button A** | GPIO 37 | 正面大按鍵，按下時為低電位 (Active LOW) |
| **按鍵 Button B** | GPIO 39 | 右側面功能鍵，按下時為低電位 (Active LOW) |
| **無源蜂鳴器** | GPIO 2 | 支援 LEDC PWM 產生擬真音效 |
| **麥克風 (SPM1423)** | CLK: GPIO 0, DATA: GPIO 34 | PDM 數位音訊輸入（與 HAT SDA 共用 GPIO 0） |

---

## 3. MiniJoyC HAT (SKU: U156) 硬體規格

### 3.1 主要規格參數
| 項目 | 規格參數 | 備註說明 |
| :--- | :--- | :--- |
| **內建微控制器** | STM32F030F4P6 (ARM Cortex-M0 @ 48MHz) | 負責採樣搖桿 ADC、驅動 RGB LED、處理 I2C 通訊 |
| **通訊介面** | I2C Slave 通訊，預設位址 `0x54` | 預設速率 100kHz ~ 400kHz |
| **操作搖桿** | 雙軸類比搖桿 (總阻值 10kΩ) | 各方向最大偏角 23°，內含中心按鍵 |
| **全彩氛圍燈** | SK6812-3535 RGB LED (1 顆) | 支援 24-bit (RGB888) 色彩控制 |
| **內建輔助電池** | 200 mAh 聚合物鋰電池 | 搭配 StickC Plus (120mAh)，總容量達 320mAh |
| **外接擴充介面** | 1 × HY2.0-4P (I2C 通訊連接埠) | 方便串接其他 Grove / Unit 模組 |
| **外型尺寸與重量** | 81.1 × 36.0 × 24.0 mm，重量約 20.6g | 雙手持握符合人體工學，握感扎實 |

### 3.2 I2C 暫存器映射手冊 (Register Map @ 0x54)

MiniJoyC 內部 STM32 韌體已將搖桿採樣、濾波與校正處理完畢，主機（ESP32）透過 I2C 直接讀取暫存器：

| 暫存器位址 | 暫存器名稱 | 長度 (Bytes) | 讀寫屬性 | 格式與數值範圍 | 功能說明 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| `0x00` | **ADC_X_REG** | 2 | R (唯讀) | 無號 12-bit (`0 ~ 4095`) | 搖桿 X 軸原始 ADC 採樣值 (低位元組在前) |
| `0x02` | **ADC_Y_REG** | 2 | R (唯讀) | 無號 12-bit (`0 ~ 4095`) | 搖桿 Y 軸原始 ADC 採樣值 (低位元組在前) |
| `0x10` | **POS_X_10BIT** | 2 | R (唯讀) | **有號 10-bit (`-512 ~ 511`)** | 已校正的 X 軸 10 位元偏移量，**中心靜止約為 0** |
| `0x12` | **POS_Y_10BIT** | 2 | R (唯讀) | **有號 10-bit (`-512 ~ 511`)** | 已校正的 Y 軸 10 位元偏移量，**中心靜止約為 0** |
| `0x20` | **POS_X_8BIT** | 1 | R (唯讀) | **有號 8-bit (`-128 ~ 127`)** | 已校正的 X 軸 8 位元偏移量，**中心靜止約為 0** |
| `0x21` | **POS_Y_8BIT** | 1 | R (唯讀) | **有號 8-bit (`-128 ~ 127`)** | 已校正的 Y 軸 8 位元偏移量，**中心靜止約為 0** |
| `0x30` | **BUTTON_REG** | 1 | R (唯讀) | `0` 或 `1` | 搖桿中心按鍵狀態：`0` = 放開，`1` = 按下 |
| `0x40` | **RGB_LED_REG** | 3 | W (唯寫) | `[Red, Green, Blue]` | SK6812 RGB 色彩資料 (RGB888 格式) |
| `0x50` ~ `0x5B` | **CAL_REG** | 12 | R/W | 6 組 16-bit 參數 | 搖桿校正參數：X_min, X_max, Y_min, Y_max, X_cen, Y_cen |
| `0xFE` | **FW_VER_REG** | 1 | R (唯讀) | 1 Byte | STM32 內部韌體版本號 |
| `0xFF` | **I2C_ADDR_REG**| 1 | R/W | 1 Byte | 變更 MiniJoyC 的 I2C 設備位址 (預設 `0x54`) |

> [!IMPORTANT]
> **數值正負方向重要提醒**：
> `POS_X_10BIT`、`POS_Y_10BIT`、`POS_X_8BIT`、`POS_Y_8BIT` 均為**有號數 (Signed Integer)**，中心點為 `0`。在程式中接收時，請務必宣告為 `int16_t` 或 `int8_t` 進行型別轉換；若宣告為無號數 (`uint8_t` / `uint16_t`)，往負方向推動時會造成負溢位（變成 `255` 或 `65535`），導致搖桿瞄準與物理小球運動產生嚴重錯誤。

---

## 4. 關鍵資源與腳位衝突防呆指南

在 M5StickC Plus 與 MiniJoyC HAT 整合時，有以下數點硬體限制與衝突防呆必須嚴格遵守：

### 4.1 雙 I2C 匯流排分工 (重要)
- **內部 I2C (Wire1)**：
  - 接腳：`SDA = GPIO 21`, `SCL = GPIO 22`。
  - 掛載元件：AXP192 (`0x34`)、MPU6886 (`0x68`)、BM8563 (`0x51`)。
  - 初始化：`M5.begin()` 自動初始化或手動呼叫 `Wire1.begin(21, 22)`。
- **頂部擴充 I2C (Wire)**：
  - 接腳：`SDA = GPIO 0`, `SCL = GPIO 26`。
  - 掛載元件：MiniJoyC HAT (`0x54`)。
  - 初始化：`Wire.begin(0, 26, 400000)` 或使用 `M5HatMiniJoyC::begin(&Wire, 0x54, 0, 26, 400000)`。
  - **防呆重點**：切勿將內部元件與 MiniJoyC 混在同一條 Wire 匯流排上，必須雙匯流排分開獨立操作。

### 4.2 GPIO 0 之物理級衝突與開機保護
- **與 SPM1423 麥克風之硬體互斥**：
  - M5StickC Plus 內建 SPM1423 麥克風的時脈腳 (CLK) 連接至 `GPIO 0`。
  - 頂部 HAT 槽的 I2C SDA 同樣使用 `GPIO 0`。
  - **重要警示**：若啟動 PDM 麥克風採樣，I2S 硬體將輸出高達 1MHz~3.2MHz 的高頻時脈，這會徹底摧毀 I2C 通訊，導致 MiniJoyC STM32 韌體接收逾時、匯流排死鎖。因此在啟用 MiniJoyC 時，**絕對嚴禁啟動 SPM1423 麥克風**。
- **ESP32 開機 Strapping Pin 風險**：
  - `GPIO 0` 同時為 ESP32 的開機模式選擇腳（Strapping Pin）。開機當下若 GPIO 0 處於低電位，ESP32 將進入 UART 韌體下載模式 (Bootloader)，導致程式無法正常開機。
  - MiniJoyC 板上有 4.7kΩ 上拉電阻至 3.3V，正常情況下為高電位；但需注意開機瞬間不要強行短路 SDA 到接地。

### 4.3 GPIO 36 與 GPIO 25 之互斥設定
- StickC Plus 內部電路設計中，`GPIO 36` 與 `GPIO 25` 共用同一個內部類比通道。
- 若程式中需使用 `GPIO 36` 作為 ADC 採樣，必須強制將 `GPIO 25` 設定為浮空（無上下拉）：
  ```cpp
  pinMode(36, INPUT);
  gpio_pulldown_dis(GPIO_NUM_25);
  gpio_pullup_dis(GPIO_NUM_25);
  ```

---

## 5. 紓壓玩具 (Stress Relief Toy) 感官互動矩陣與模式規劃

本硬體組合具備極高維度的回饋管道，非常適合打造「掌上型多功能電子解壓神器」：

### 5.1 感官互動維度對應表
| 感官維度 | 輸入 (Input) 來源 | 輸出 (Output) 回饋 | 紓壓體驗亮點 |
| :--- | :--- | :--- | :--- |
| **觸覺 (Tactile)** | 雙軸搖桿 (推動阻尼感/回彈力道)、搖桿中心微動開關、Button A/B 清脆微動按鍵 | 無（機械結構本體手感） | 滿足「手部無聊愛按筆、撥開關」的微動作強迫症需求 |
| **聽覺 (Auditory)** | — | GPIO 2 無源蜂鳴器 (PWM 輸出) | 模擬機械鍵盤軸體青軸/茶軸喀噠聲、敲擊電子木魚「咚」聲、放鬆白噪音 |
| **視覺 (Visual)** | — | 1.14" ST7789v2 LCD (全彩 60FPS 雙緩衝) + SK6812 全彩 RGB LED | 柔和呼吸漸層光圈、戳破泡泡碎裂動效、陀螺旋轉流光、滿意度量表 |
| **動態體感 (Kinetic)** | MPU6886 六軸 IMU (晃動、翻轉、傾斜) | 畫面小球重力響應、色彩加速度連動 | 甩動解壓、電子搖搖樂、擲骰子、平衡迷宮走珠 |

### 5.2 推薦紓壓模式矩陣 (Modes Proposal)

```
[模式選擇] ─── Button B 循環切換
  │
  ├── 1. 數位電子木魚 (Cyber Wooden Fish)
  │    ├── 輸入：點擊 Button A 或按壓搖桿中心鍵
  │    ├── 視覺：螢幕木魚晃動放大，彈出「功德 +1」、「焦慮 -1」浮空飄移字樣
  │    ├── 燈光：SK6812 閃爍金黃色溫暖光芒
  │    └── 聽覺：蜂鳴器發出深沉擬真的「咚～」共振敲擊聲
  │
  ├── 2. 無限氣泡紙 (Infinite Bubble Popper)
  │    ├── 輸入：推動搖桿瞄準氣泡，按下中心鍵或 Button A 戳破
  │    ├── 視覺：135x240 LCD 繪製逼真彩色氣泡陣列，戳破瞬間伴隨碎裂粒子特效
  │    ├── 燈光：對應氣泡顏色的 RGB 爆裂閃爍
  │    └── 聽覺：超爽快高頻「啵 (Pop!)」清脆破裂音效
  │
  ├── 3. 虛擬指尖陀螺 (Virtual Fidget Spinner)
  │    ├── 輸入：利用搖桿快速劃圈撥動，或以手腕帶動 StickC 甩動 (IMU 偵測角速度)
  │    ├── 視覺：螢幕呈現高幀率旋轉指尖陀螺，具備動態模糊與阻尼減速物理模型
  │    ├── 燈光：SK6812 伴隨轉速漸變（慢速深藍 -> 中速亮綠 -> 極速烈火紅）
  │    └── 聽覺：蜂鳴器發出隨轉速連續平滑變頻之旋轉嗡嗡聲 (Whirring Sound)
  │
  ├── 4. 深呼吸引導儀 (4-7-8 Breathing Guide)
  │    ├── 目的：心率平復、舒緩焦慮與壓力
  │    ├── 視覺：LCD 呈現平滑膨脹與收縮的禪意光環（吸氣 4 秒 -> 閉氣 7 秒 -> 呼氣 8 秒）
  │    ├── 燈光：SK6812 呈現緩慢柔和的天空藍與草綠呼吸漸層
  │    └── 聽覺：極低音階柔和提示音，引導節奏切換
  │
  └── 5. 虛擬重力彈珠盤 (Gravity Ball Sandbox)
       ├── 輸入：傾斜機身 (MPU6886 加速度感應) 或撥動搖桿施加外力
       ├── 視覺：多顆物理彈性小球在螢幕內碰撞、摩擦與彈跳 (135x240 Sprite 雙緩衝 60 FPS)
       ├── 燈光：小球撞牆瞬間觸發 RGB 撞擊光效
       └── 聽覺：每次碰撞產生微小的「噠、滴」音效，手感極為療癒
```

---

## 6. 本專案已備妥之必要資源檔案清單

所有硬體數據手冊、原理圖與專用驅動函式庫皆已保存在本專案目錄中，以供後續直接編譯引用：

1. **原理圖與官方手冊** (`docs/hardware/datasheets/`)：
   - `StickC-Plus-Schematic.pdf`：M5StickC Plus 完整電路原理圖 (電源架構、LCD SPI 線路、PMU 接線)
   - `MiniJoyC-HAT-Schematic.pdf`：MiniJoyC 擴充底座原理圖 (STM32 接線、搖桿分壓電路、SK6812 供電)
   - `MiniJoyC-I2C-Protocol.pdf`：MiniJoyC 官方 I2C 通訊協議與暫存器規範
   - `M5StickC-Plus-Doc.pdf`：M5StickC Plus 完整繁體中文硬體與軟體說明手冊
2. **獨立驅動程式庫** (`lib/M5HatMiniJoyC/`)：
   - `M5HatMiniJoyC.h`：MiniJoyC C++ 驅動類別標頭檔（支援 8-bit / 10-bit 位置讀取、ADC 讀取、RGB 設定與校正）
   - `M5HatMiniJoyC.cpp`：驅動實作原始碼（已修正 `_wire->read()` 實體引用並支援自訂匯流排）
   - `README.md`：MiniJoyC 官方說明與校正範例

---
*文件編制：Antigravity AI Assistant*
*專案路徑：`c:/laragon/www/m5stickc-withjoyc`*
